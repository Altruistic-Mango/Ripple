var React = require('react');

var LoginContent = React.createClass({
    render: function () {
      return (
        <div className="home">
          <h1>Login Content</h1>
        </div>
      );
    }

  });

module.exports = LoginContent;